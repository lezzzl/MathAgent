# КОНТРОЛЬ А: базовая линия на SGLang, зерно 42.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-sglang-s42/.
#
# Зачем ещё один прогон на том же зерне, что и nikita-qwen4b-v11-aime26-s42.
# Тот прогон шёл СТАРОЙ командой (scripts/bench_qwen4b.sh, коммит 70f7fed): без
# --vote-tiebreak-samples 0 и с --workers 16. Сравнивать с ним что-либо нельзя —
# конфигурация другая. Этот файл задаёт эталон, относительно которого меряются
# соседний nikita-qwen4b-v11-aime26-evalrandom-s42b — случайный оценщик.
#
# Две спеки отличаются ровно одним пунктом. Зерно и там и там одно.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 42
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
