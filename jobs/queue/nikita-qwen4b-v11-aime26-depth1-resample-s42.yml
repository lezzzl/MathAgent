# AIME26: оценщик с глубины 1 + добор ВСЕГДА. Зерно 42.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-depth1-resample-s42/.
#
# Последняя незаполненная клетка сетки. Отличие от
# nikita-qwen4b-v11-aime26-evaldepth1-s42 (24/30) ровно одно:
# --resample-token-threshold 0 вместо 250000 — каждая задача получает все три
# сэмпла, а не только та, где первый выглядел ненадёжным.
#
# Зачем. Что уже известно по зёрнам 42 и 43 (60 задач):
#
#   база                                  46/60   48.09M токенов
#   глубина 1, добор адаптивный           48/60   35.70M
#   глубина 2, добор адаптивный           42/60   28.55M
#   глубина 2, добор ВСЕГДА               44/60   43.94M
#
# Полный добор на глубине 2 не вернул потерянного: +2 задачи при p = 0.79, зато
# расход вырос почти до базового. То есть голосование не заменяет вердикт
# оценщика. Но глубина 1 ничего не теряет и экономит четверть бюджета — и вот эти
# сэкономленные токены имеет смысл вложить в голосование.
#
# Ожидаемый расход: ~90 сэмплов вместо ~61, то есть порядка 25M токенов на зерно
# (0.275M на сэмпл по прошлому прогону) — примерно уровень базы.
#
# Как читать. Счёт выше 48/60 при расходе на уровне базы означает конфигурацию,
# которая лучше базовой по обеим осям сразу — такого у нас ещё не было. Счёт
# около 48/60 означает, что голосование упирается в потолок и на глубине 1 тоже,
# и тогда лучшим остаётся адаптивный добор как более дешёвый.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 42
  --samples 3
  --resample-token-threshold 0
  --vote-tiebreak-samples 0
  --evaluator-min-depth 1
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
