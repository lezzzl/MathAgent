# AIME26 на золотой конфигурации qwen4b-v11, зерно 45.
# Имя файла = run_id → результаты лягут в results/runs/nikita-qwen4b-v11-aime26-s45/.
#
# Один из четырёх одинаковых файлов (зёрна 42/43/44/45). Разнесены по отдельным
# спекам, а не собраны в цикл: карт три, и так три зерна считаются одновременно;
# падение одного не уносит остальные; результаты каждого публикуются по его
# завершении, а не после последнего.
#
# Зачем четыре зерна. Разброс от прогона к прогону — ±9 задач из 30: из 90 задач
# 54 решаются всегда, 29 плавают, 7 не решались ни разу. Одним прогоном правку
# промпта измерить нельзя, поэтому меряем полосами (scripts/report_bands.py).
#
# --base-url только плейсхолдером: порт выдаёт диспетчер, и run_benchmarks.sh
# отклоняет команду с зашитым адресом (require_no_hardcoded_address). {RUN_ID} и
# {SERVED_MODEL_NAME} он подставит там же.
#
# Серверные ручки — под 4B, а не под дефолты скрипта (они настроены на 9B):
#   MODEL/SERVED_MODEL_NAME  4B; имя = путь модели, иначе require_served_model
#                            не найдёт её в /models;
#   CONCURRENCY=96           4B держит куда больший батч, чем 9B; отсюда
#                            MAX_NUM_SEQS=144 на стороне сервера;
#   MAX_MODEL_LEN=65536      как в том прогоне; ниже 52000 лимит генератора
#                            (40000) не влезает и шаги начнут обрываться;
#   KV_CACHE_DTYPE=auto      дефолт fp8_e4m3 меняет численность KV-кэша, а мы
#                            воспроизводим замер, а не ускоряем его;
#   MTP=0                    спекулятивный декод (NEXTN) у 4B не проверялся, на
#                            модели без MTP-слоя сервер не поднимется;
#   TOOL_CALL_PARSER         §2.18: на hermes сервер не разобрал ни одного вызова
#                            инструмента из 228, на qwen3_coder — все.
#
# --workers 30, а не 96: в бенчмарке 30 задач, сэмплы внутри задачи идут строго
# по очереди (_solve_with_graph — обычный while), и больше 30 запросов
# одновременно пайплайн не создаёт при всём желании. CONCURRENCY=96 задаёт
# MAX_NUM_SEQS=144 на стороне сервера — с запасом относительно этих 30.
#
# --vote-tiebreak-samples 0 — возврат к тому, что было 2026-08-07. Кворум
# считается как «≥2 голоса и первое место одно», поэтому ОДИН ответ его не даёт:
# задача, которую добор признал уверенной, всё равно уходила на второй сэмпл. В
# дымовом прогоне это видно дословно — «первый сэмпл уверенный — добор не нужен»
# и сразу «большинства нет — добираю». Механизм появился 2026-08-10, то есть
# ПОСЛЕ прогона на 28/30, и удваивает цену уверенных задач (в том прогоне таких
# было 23 из 30). Для замера разброса по зёрнам нужна та же конфигурация, что
# дала 28/30, поэтому ничья не разрешается добором.
#
# Прогон 2026-08-07 на этой конфигурации: 28/30, 12.39M токенов, 30.3 машино-часа.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 45
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
