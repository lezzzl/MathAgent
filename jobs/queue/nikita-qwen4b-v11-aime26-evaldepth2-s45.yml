# ЭКСПЕРИМЕНТ 2, четвёртая точка: оценщик с глубины 2, зерно 45.
#
# База для этого зерна уже есть: results/runs/nikita-qwen4b-v11-aime26-s45/, 23/30.
# Тот прогон шёл ровно текущей командой (проверено по его runner.log), поэтому
# четвёртая точка стоит один прогон, а не два.
#
# Зачем. По трём зёрнам 69/90 против 64/90, p = 0.36, но 95% интервал на разницу
# — от -4.9 до +14.2 п.п. Слишком широко, чтобы делать --evaluator-min-depth 2
# дефолтом: интервал допускает потерю четырнадцати процентных пунктов. Расход при
# этом падает на 47%, так что решение стоит реальных денег и заслуживает узкого
# интервала.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-evaldepth2-s45/.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 45
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --evaluator-min-depth 2
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
