# ЯВНОЕ СЭМПЛИРОВАНИЕ: top_p=1.0, top_k=-1, min_p=0. Зерно 42.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-sampling-s42/.
#
# Отличие от базовой линии nikita-qwen4b-v11-aime26-sglang-s42b ровно одно:
# --sampling top_p=1.0,top_k=-1,min_p=0. База на этом зерне даёт 23/30.
#
# Зачем. Клиент всегда слал в запрос только temperature и max_tokens. top_p,
# top_k и min_p подставляет ДВИЖОК, у каждого свои: у SGLang в логе видно
# sampling_defaults='model', то есть берётся generation_config модели. Что
# подставлял vLLM в докере, где получены 28/30, неизвестно и невосстановимо.
#
# Сверка показала, что агентская часть конфигурации совпадает с золотым прогоном
# ПОБАЙТОВО: системные промпты всех трёх ролей дают те же sha1, вызовы шли на тех
# же num_predict, те же seed/samples/порог/бюджет. Различие целиком серверное, и
# сэмплирование — единственный его механизм, который мы можем задать сами, не
# уходя с SGLang.
#
# Значения нейтральные: top_p=1.0, top_k=-1 и min_p=0 отключают отсечение, и
# остаётся чистое сэмплирование по температуре — то, чем исторически был дефолт
# vLLM. Температура остаётся своя у каждой роли, её флаг не трогает.
#
# Как читать:
#   счёт вернулся к ~28 -> дело в дефолтах движка, фиксируем их навсегда;
#   счёт остался ~23    -> сэмплер ни при чём, подозрение переходит на
#                          префиксный кэш (у нас включён, в докере неизвестно).
#
# Ценность прогона не только в этих пяти задачах. Сейчас конфигурация
# недоопределена: часть параметров генерации задаём не мы. При любой смене
# версии SGLang или весов модели замеры поедут снова, и причину опять будет не
# назвать. Явное задание закрывает это независимо от результата.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 42
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --sampling top_p=1.0,top_k=-1,min_p=0
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
