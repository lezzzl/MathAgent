# ДЫМОВОЙ ПРОГОН: две задачи AIME26 на золотой конфигурации qwen4b-v11.
# Имя файла = run_id → результаты лягут в results/runs/nikita-qwen4b-v11-aime26-smoke/.
#
# Смысл не в числах, а в проверке обвязки до того, как мы займём карты на четыре
# многочасовых прогона. Локально проверить нельзя ничего из этого: находится ли
# 4B, тянется ли датасет с HF, поднимает ли SGLang tool-call-parser, доезжает ли
# per-role enable_thinking. Две задачи — минут десять.
#
# Конфигурация целиком лежит в scripts/bench_qwen4b.sh: диспетчер пропускает в
# задачу только переменные из белого списка, а нам нужны KV_CACHE_DTYPE, MTP и
# TOOL_CALL_PARSER, которых там нет.
#
# Что смотреть в логе:
#   [probe] enable_thinking УЧТЁН      — per-role настройки v11 работают
#   [config] pipeline=qwen4b … v11.yml — подхватились пайплайн и промпт
#   [preflight] … max_model_len=65536  — контекст сервера
#   [tool_call #N] / [tool_result]     — инструменты исполняются
#   [TOOL SALVAGE]                     — сервер вызовы НЕ разбирает, парсер не доехал
#   [verify] ИТОГ AIME26: N/2          — автосверка отработала
#
# Перед боевыми зёрнами этот файл удалить, иначе он поедет вхолостую вместе с ними.
command: bash scripts/bench_qwen4b.sh aime26 42 --limit 2
gpus: 1
timeout_minutes: 60
