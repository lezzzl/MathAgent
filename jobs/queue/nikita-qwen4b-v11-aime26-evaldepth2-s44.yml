# ЭКСПЕРИМЕНТ 2, третья точка: оценщик с глубины 2, зерно 44.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-evaldepth2-s44/.
#
# Отличие от базовой линии nikita-qwen4b-v11-aime26-sglang-s44 ровно одно:
# --evaluator-min-depth 2. Шаги на глубинах 0 и 1 принимаются без вызова модели.
#
# Зачем третья точка. По двум зёрнам счёт разошёлся в разные стороны:
#
#   зерно 42:  23/30 -> 18/30   (-5)
#   зерно 43:  23/30 -> 24/30   (+1)
#
# Итого 46/60 против 42/60, p = 0.42 — вывод по счёту сделать нельзя, разброс
# между зёрнами больше самого эффекта. При этом расход изменился однозначно и
# сильно: токенов -41%, кругов восстановления -47%, агент доходит до глубины 7
# вместо 5. То есть цена вопроса высокая, а знак эффекта неизвестен.
#
# Как читать: если зерно 44 ляжет рядом с 43 (счёт держится) — получаем готовую
# оптимизацию, минус 41% бюджета даром, и вся польза оценщика оказывается в
# меньшинстве его вызовов. Если ляжет рядом с 42 — ранняя строгость оправдана,
# и узкое место не в оценщике, а в генераторе, который на первых шагах выдаёт
# брак в двух случаях из трёх.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 44
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --evaluator-min-depth 2
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
