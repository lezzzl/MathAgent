# ЭКСПЕРИМЕНТ 1: случайный оценщик, зерно 42.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-evalrandom-s42b/.
#
# Единственное отличие от nikita-qwen4b-v11-aime26-evalrandom-s42b —
# --evaluator-mode random: модель для оценки шага не зовётся вовсе, балл берётся
# из той же шкалы (0/0.25/0.5/0.75/1.0) со средней долей отвержений 60% — как у
# живого оценщика в этих же прогонах (68-71%).
#
# Доля отвержений подогнана намеренно. Со случайной равномерной шкалой мы
# сравнивали бы строгого судью с мягким, а не оценку со случайностью, и разница
# в счёте ничего не сказала бы про качество вердикта.
#
# Как читать результат:
#   счёт заметно упал  -> вердикт нёс информацию, оценщик оправдан;
#   счёт не изменился  -> четверть бюджета уходит на шум, и вся ветка
#                         «оценщик как фильтр» под вопросом.
#
# Балл воспроизводим: зерно берётся из той же цепочки (seed + task_id + номер
# вызова), что и у обращений к модели.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 42
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --evaluator-mode random
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
