# Оценщик с глубины 1 на AIME 2025, зерно 42. Переносимость лучшего конфига.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime25-evaldepth1-s42b/.
#
# База есть: results/runs/nikita-qwen4b-v11-aime25-sglang-s42/, 18/30.
#
# Зачем. Глубина 2 на AIME25 просела так же, как на AIME26 (18/30 -> 14/30), то
# есть потеря переносится на другой набор задач. Про глубину 1 мы этого не знаем
# — а именно её собираемся сделать дефолтом и уже запустили на ней IMO.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime25.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 42
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --evaluator-min-depth 1
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime25.jsonl
gpus: 1
timeout_minutes: 720
