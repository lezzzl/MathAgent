# AIME26: добор ВСЕГДА + оценщик с глубины 2. Зерно 42.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-depth2-resample-s42/.
#
# Отличие от nikita-qwen4b-v11-aime26-evaldepth2-s42 ровно одно:
# --resample-token-threshold 0 вместо 250000. Тот прогон дал 18/30.
#
# Что это меняет. При пороге 250000 второй и третий сэмплы запускались только для
# задач, где первый выглядел ненадёжным: сдался, не дал ответа или сжёг больше
# порога. В прогонах с глубины 2 это давало ~50 сэмплов на 30 задач, то есть
# больше половины задач решались один раз и голосования не видели вовсе.
# С порогом 0 каждая задача получает все три сэмпла и полноценное голосование.
#
# Зачем именно на глубине 2. Она экономит 48% бюджета, но стабильно теряет
# задачи. Голосование — единственный механизм за всю историю проекта, который
# пробивал «безнадёжную» полосу. Вопрос: возвращает ли полный добор то, что
# отняла глубина 2, и во что это обходится.
#
# Ожидаемый расход: ~90 сэмплов вместо ~50, то есть порядка 24M токенов против
# 14M у адаптивного добора — примерно на уровне базовой конфигурации (25M),
# только бюджет потрачен на голосование, а не на оценщика.
#
# ВАЖНО про порог 0. До 2026-08-17 он работал наоборот: в коде стояло
# `threshold and tokens > threshold`, и ноль как ложное значение ВЫКЛЮЧАЛ
# проверку расхода — добор случался только у сдавшихся. Исправлено вместе с этой
# спекой; прогоны с порогом 0 до этой даты означают не то, что написано в справке.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 42
  --samples 3
  --resample-token-threshold 0
  --vote-tiebreak-samples 0
  --evaluator-min-depth 2
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
