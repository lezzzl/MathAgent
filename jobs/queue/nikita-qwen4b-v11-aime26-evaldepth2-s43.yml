# ЭКСПЕРИМЕНТ 2: оценщик включается только с глубины 2. Зерно 43.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-evaldepth2-s43/.
#
# Отличие от базовой линии nikita-qwen4b-v11-aime26-sglang-s43b ровно одно:
# --evaluator-min-depth 2. Шаги на глубинах 0 и 1 принимаются без вызова модели.
#
# Зачем именно 2. Замер по двум базовым прогонам (529 оценок):
#
#   глубина 0:  237 оценок, отвергнуто 69%
#   глубина 1:  201 оценок, отвергнуто 66%
#   глубина 2:   72 оценки,  отвергнуто 49%
#   глубина 3+:  19 оценок,  отвергнуто 42%
#
# На глубинах 0-1 живёт 83% всех оценок и 87% всех отвержений, и строгость там
# заметно выше. Эксперимент 1 уже показал, что оценщик в целом окупается
# (46/60 против 38/60 со случайным). Этот прогон проверяет, окупается ли именно
# ранняя строгость — та самая, что даёт почти все отвержения.
#
# Как читать:
#   счёт не упал -> ранние отвержения были шумом, и оценщика надо звать позже;
#   счёт упал    -> строгость на первых шагах оправдана, оценщик не галлюцинирует.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 43
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --evaluator-min-depth 2
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
