# Оценщик с глубины 1, зерно 44. Третья точка лучшего конфига.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-evaldepth1-s44b/.
#
# База для зерна есть: results/runs/nikita-qwen4b-v11-aime26-sglang-s44/, 23/30.
# Отличие ровно одно: --evaluator-min-depth 1.
#
# Зачем. Конфиг становится дефолтом (EXPERIMENTS.md §2б: 48/60 против базы 46/60
# при расходе -26%), но стоит он пока на двух зёрнах. У базы их четыре, у глубины
# 2 — пять. Решение, которое мы понесём дальше, должно опираться на столько же.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 44
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --evaluator-min-depth 1
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
