# ЭКСПЕРИМЕНТ 1, четвёртая точка: случайный оценщик, зерно 45.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-evalrandom-s45b/.
#
# Отличие от базы ровно одно: --evaluator-mode random. Модель для оценки шага не
# зовётся, вердикт бросается монетой по бинарной шкале 0.0/1.0 с долей нулей 60%
# — как у живого оценщика (60-66% в базовых прогонах).
#
# БАЗА ДЛЯ ЭТОГО ЗЕРНА УЖЕ ЕСТЬ: results/runs/nikita-qwen4b-v11-aime26-s45/,
# 23/30. Несмотря на старое имя, тот прогон шёл РОВНО текущей командой — он
# стартовал дважды, и второй раз уже новой спекой, с --vote-tiebreak-samples 0 и
# --workers 30. Подтверждается и распределением сэмплов {1: 10, 3: 20}: ни одной
# задачи с 2 или 4 сэмплами, то есть добор при ничьей был выключен, как и здесь.
# Поэтому четвёртая точка стоит один прогон, а не два.
#
# Зачем. По трём зёрнам 69/90 против 60/90, p = 0.049 — порог пройден впритык.
# Ожидание при том же поведении: 17 потеряно против 5 взятых из 22 пар, p ≈ 0.017.
# Но это НЕ формальность: если зерно 45 ляжет в другую сторону (скажем, 1
# потеряно и 4 взято), выйдет 14 против 8 и p ≈ 0.29 — вывод развалится. Прогон
# способен как закрыть эксперимент, так и отменить его результат.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 45
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --evaluator-mode random
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
