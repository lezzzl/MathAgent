# ЭКСПЕРИМЕНТ 2б: оценщик с глубины 1 (пропускается только глубина 0). Зерно 42.
#
# Отличие от базы nikita-qwen4b-v11-aime26-sglang-s42b (23/30) ровно одно:
# --evaluator-min-depth 1.
#
# Зачем отдельно от глубины 2. Разбивка по базовым прогонам:
#
#   глубина 0: 360 оценок, отвергнуто 70%
#   глубина 1: 293 оценки,  отвергнуто 66%
#
# Обе зоны выглядят одинаково строгими, но это не значит, что обе бесполезны.
# Глубина 0 — это первый шаг из пустого контекста, глубина 1 уже опирается на
# принятый шаг. Если min-depth 1 сохраняет счёт при экономии, близкой к min-depth
# 2, то он — более безопасный кандидат в дефолт: рискуем меньшим.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-evaldepth1-s42/.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 42
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --evaluator-min-depth 1
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
