# AIME26 на золотой конфигурации qwen4b-v11, зерно 43.
# Имя файла = run_id → результаты лягут в results/runs/nikita-qwen4b-v11-aime26-s43/.
#
# Один из четырёх одинаковых файлов (зёрна 42/43/44/45). Разнесены по отдельным
# спекам, а не собраны в цикл внутри одной, по трём причинам: карт три, и так три
# зерна считаются одновременно; падение одного зерна не уносит остальные;
# результаты каждого публикуются по его завершении, а не после последнего — то
# есть таймаут не может обнулить уже посчитанное.
#
# Зачем четыре зерна. Разброс от прогона к прогону — ±9 задач из 30: из 90 задач
# 54 решаются всегда, 29 плавают, 7 не решались ни разу. Одним прогоном правку
# промпта измерить нельзя, поэтому меряем полосами (scripts/report_bands.py).
#
# Вся конфигурация — в scripts/bench_qwen4b.sh, включая причины каждого значения.
# Повторяет прогон 2026-08-07: AIME26 28/30, 12.39M токенов, 30.3 машино-часа.
# При --workers 16 это ~2 часа на зерно; 720 минут — четырёхкратный запас.
command: scripts/bench_qwen4b.sh aime26 43
gpus: 1
timeout_minutes: 720
