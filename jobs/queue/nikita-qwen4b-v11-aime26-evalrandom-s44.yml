# ЭКСПЕРИМЕНТ 1, третья точка: случайный оценщик, зерно 44.
# Имя файла = run_id → results/runs/nikita-qwen4b-v11-aime26-evalrandom-s44/.
#
# Отличие от nikita-qwen4b-v11-aime26-sglang-s44 ровно одно:
# --evaluator-mode random. Модель для оценки шага не зовётся, вердикт бросается
# монетой по бинарной шкале 0.0/1.0 с долей нулей 60% — как у живого оценщика
# (63-66% в базовых прогонах).
#
# Итог по двум зёрнам: база 46/60, случайный 38/60, из них 10 задач потеряно и
# 2 взято. Механизм — случайный пропускает на 42% больше шагов и решает меньше.
command: >-
  MODEL=Qwen/Qwen3.5-4B
  SERVED_MODEL_NAME=Qwen/Qwen3.5-4B
  CONCURRENCY=96
  MAX_MODEL_LEN=65536
  KV_CACHE_DTYPE=auto
  MTP=0
  TOOL_CALL_PARSER=qwen3_coder
  scripts/run_benchmarks.sh --
  .venv/bin/python -u scripts/benchmarks/run_aime26.py
  --model {SERVED_MODEL_NAME}
  --base-url {BASE_URL}
  --api-key EMPTY
  --pipeline qwen4b
  --prompt conf/base/prompts/agent-step-qwen4b-v11.yml
  --branch-mode single
  --seed 44
  --samples 3
  --resample-token-threshold 250000
  --vote-tiebreak-samples 0
  --evaluator-mode random
  --no-verify-step
  --token-budget 800000
  --timeout 1200
  --workers 30
  --output results/runs/{RUN_ID}/aime26.jsonl
gpus: 1
timeout_minutes: 720
