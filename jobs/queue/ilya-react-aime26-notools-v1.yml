# Ablation: react-агент БЕЗ тулов на aime26 (benchmarks.react.tools=none).
# Отвечает на «правда ли с тулами решается ДРУГОЙ набор задач?»: сравним набор
# верных с этого прогона против прогона с тулами (aime26 = 93.3%, ilya-react-full-v1)
# через scripts/analyze_runs.py --compare.
# Имя файла = run_id → results/runs/ilya-react-aime26-notools-v1/aime26.jsonl.
#
# tools=none → промпт без протокола инструментов (голое reasoning в react-обвязке),
# tester выключен (без базовых тулов он бессмыслен). Остальное — как в основном
# прогоне: динамический max_tokens, max_steps=8, sglang 48 seqs.
# 30 задач ≈ одна волна → timeout 120 с запасом.
command: "ENGINE=sglang MAX_MODEL_LEN=131072 CONCURRENCY=32 scripts/run_benchmarks.sh -- .venv/bin/kedro run --pipeline benchmarks --params benchmarks.pipeline=react,benchmarks.react.tools=none,benchmarks.react.tester=false,benchmarks.prompt=conf/base/prompts/react-tools.yml,benchmarks.model.name={SERVED_MODEL_NAME},benchmarks.model.base_url={BASE_URL},benchmarks.select=[aime26],benchmarks.generation.max_tokens=90112,benchmarks.runtime.concurrency=32,benchmarks.serving.vllm_max_num_seqs=48,benchmarks.serving.vllm_max_model_len=131072,benchmarks.run_id={RUN_ID}"
gpus: 1
timeout_minutes: 120
