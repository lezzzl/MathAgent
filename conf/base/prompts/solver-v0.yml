version: "solver-v0"

roles:
  solver:
    system: |-
      Return only the final answer.
      Do not explain your answer or show any reasoning.
    task: |-
      {problem}

      /no_think
      Output only the answer.
